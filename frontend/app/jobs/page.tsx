"use client";
import { useQuery } from "@tanstack/react-query"; import { RotateCw } from "lucide-react"; import { api } from "@/lib/mock-api"; import { PageHeader } from "@/components/page-header"; import { SearchBox, StatusPill } from "@/components/ui";
export default function Jobs(){const {data=[]}=useQuery({queryKey:["jobs"],queryFn:api.getJobs,refetchInterval:5000});return <><PageHeader eyebrow="Operations" title="Job monitor" description="A real-time view of every automated content job."/><div className="panel overflow-hidden"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-white/[.07] p-4"><SearchBox placeholder="Find a job..."/><span className="text-xs text-zinc-500">Auto-refreshing every 5s</span></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-white/[.07] text-xs uppercase tracking-wider text-zinc-500"><tr><th className="px-5 py-3 font-medium">Job</th><th className="px-5 py-3 font-medium">Current stage</th><th className="px-5 py-3 font-medium">Started</th><th className="px-5 py-3 font-medium">Duration</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3"/></tr></thead><tbody className="divide-y divide-white/[.05]">{data.map(j=><tr key={j.id} className="hover:bg-white/[.025]"><td className="px-5 py-4"><p className="font-medium">{j.id}</p><p className="mt-1 text-xs text-zinc-500">{j.title}</p></td><td className="px-5 py-4 text-zinc-300">{j.stage}</td><td className="px-5 py-4 text-zinc-500">{j.started}</td><td className="px-5 py-4 font-mono text-xs text-zinc-400">{j.duration}</td><td className="px-5 py-4"><StatusPill status={j.status}/></td><td className="px-5 py-4">{j.status==="failed"&&<button className="inline-flex items-center gap-1.5 text-xs text-violet-300"><RotateCw className="h-3.5 w-3.5"/>Retry</button>}</td></tr>)}</tbody></table></div></div></>}
